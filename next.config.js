/**
 * Configure Next.js to produce a static export using `output: 'export'`.
 * Run `npm run build` and the build output will be exported for static hosting.
 */
module.exports = {
  output: 'export',
};
